# Client data — real Swedish bookkeeping PII (client roster, org numbers, plate
# ledger, per-client manuals). Kept local only (and in ~/Backups/claude-bokforing);
# NEVER pushed to GitHub.
references/
skills/*/references/

# macOS / editor cruft
.DS_Store

# Local-only PII denylist used by .githooks/pre-commit (real client names) —
# never pushed. pii-denylist.example.txt (committed) documents the format.
.githooks/pii-denylist.txt

# Python bytecode
__pycache__/
*.pyc
