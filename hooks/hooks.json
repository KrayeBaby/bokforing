{
  "description": "bokforing safety hook: blocks Spiris/Visma ledger-writing Bash commands (--apply / --prod / write probes) that bypass the review, freshness, batch-binding or production gates.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/guard-spiris-apply.py\""
          }
        ]
      }
    ]
  }
}
